# Render Blueprint - Using Docker
services:
  - type: web
    name: ao3-randomizer
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SECRET_KEY
        generateValue: true
